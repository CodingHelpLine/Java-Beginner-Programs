# Java Simple Text Analyzer program
-------------------------------------------------------
Parse input file. Read text. Remove punctuations to normalize
the text and then count letters and words Frequency using Core Java 